// Create a new file, e.g., Plant.ts
interface Plant {
    name_authority: string; // Replace with the actual type returned by the API for id
    common_names: string; // Replace with the actual type returned by the API for title
    image: string; // Replace with the actual type returned by the API for imageSrc
    description: string; // Replace with the actual type returned by the API for description
  }
  
  export default Plant;
  